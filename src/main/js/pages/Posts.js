import React from "react";
import Axios from "axios";

export default class Posts extends React.Component {
    constructor(props) {
        super(props);
    }

    getPostsData() {
        Axios.get()
    }
}